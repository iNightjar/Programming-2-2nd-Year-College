package programming2;
import java.util.*;
public class chapter7 {
    /*
    public static void main(String[] args) {
        // create cylinder object and display it's properties
        cylinder cylinder1 = new cylinder();
        // assign values to cylinder length and radius
        Scanner input = new Scanner(System.in);
        System.out.println("Enter cylinder length");
        double length = input.nextDouble();
        System.out.println("Enter cylinder radius");
        double radius = input.nextDouble();
        cylinder1.setLength(length);
        cylinder1.setRadius(radius);
        System.out.println("the length is : " + cylinder1.getLength());
        System.out.println("the radius is : " + cylinder1.getRadius());
        System.out.println("the volume of the cylinder is : " + cylinder1.findVolume());
        System.out.println("the area of the cylinder is : " + cylinder1.findArea());
    }
}
class cylinder extends circle {
    // cylinder extends circle
    private double length = 1;

    // return length
    public double getLength() {
        return length;
    }

    // set length
    public void setLength(double newLength) {
        this.length = newLength;
    }

    // return the volume of this cylinder
    public double findVolume() {
        return super.findArea() * length;
    }

    public double findArea() { // trying the method overloading here ;)
        return super.findArea()  + 100;
    }
}
    class circle {
        private double radius = 0;

        public void setRadius(double newradius) {
            this.radius = newradius;
        }

        public double getRadius() {
            return radius;
        }

        public double findArea() {
            return radius * radius * Math.PI;
        }*/
}
