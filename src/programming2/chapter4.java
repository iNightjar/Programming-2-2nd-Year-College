package programming2;
import java.util.*;
/*
// Circle class defined with two constructors
class simpleCircle{
    double radius;
    //  construct a circel with radius 1
    simpleCircle()  {
        radius = 1;
    }
    // construct a circle with  a specified radius
    simpleCircle (double newRadius){
        radius = newRadius;
    }
    // return the area of this circle
    double getArea(){
        return radius * radius  * Math.PI;
    }
    // Return the perimeter of this circle
    double getPerimeter(){
        return 2 * radius * Math.PI;
    }
    // set a new radius for this circle
    void setRadius(double newRadius)
    {
        radius = newRadius;
    }
}
public class chapter4 {
    public static void main(String[] args) {
        // create a circle with radius 1
        simpleCircle circle1   = new simpleCircle();
        System.out.println("the area of the circle of radius : " + circle1.radius +
                " is " + circle1.getArea());
        simpleCircle circle2   = new simpleCircle(25);
        System.out.println("the area of the circle of radius : " + circle2.radius +
                " is " + circle2.getArea());
    }
}*/

